package app.kwlee.authentica.presentation.home

enum class HomeError {
    FillRequiredFields,
    UriRequired,
    UpdateAccountFailed,
    DeleteAccountFailed,
    ReorderAccountsFailed,
    AddAccountFailed,
    InvalidUri
}
